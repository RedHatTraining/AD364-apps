package com.demos.event_planner;

import java.lang.Number;
import java.lang.String;

rule "create-table-for-guest"
	dialect "mvel"
	ruleflow-group "seat-guests"
	when
		Event( $tableSize : tableSize != null )
		Guest( $partyName : party != null )
		not (Table( name == $partyName , guests.size() < $tableSize )) 
	then
		Table table = new Table();
		table.setName( $partyName );
		insert( table );
end
